# ./annos stores the sugggested labels
   each video has a folder and each frame has a json file

# ./labels stores the answers from turkers
   each video has a folder and each frame has a json file
	


